# Installation

Structorex is built with Python and published on PyPI. It requires Python 3.8 or higher.

## Global Installation (Recommended)

You can install it easily using pip:

```bash
pip install structorex
```

## From Source

```bash
git clone https://github.com/yourusername/structorex.git
cd structorex
pip install -e .
```
